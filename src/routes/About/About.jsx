import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './About.styles';

const AboutRouteContainerComponent = (props) => (
  <div className="AboutWrapper">
    Test content
  </div>
);

AboutRouteContainerComponent.propTypes = {
  // bla: PropTypes.string,
};

AboutRouteContainerComponent.defaultProps = {
  // bla: 'test',
};

export default AboutRouteContainerComponent;
